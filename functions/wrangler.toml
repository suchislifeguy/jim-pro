name = "jim-pro-ai"
main = "index.js"
compatibility_date = "2025-01-01"

kv_namespaces = [
  { binding = "QUOTA_KV", id = "adadd7d29c384c30817c92537d337a00" }
]

[vars]
TESTER_UIDS = "tester1,tester2,tester3,0UBVoe862PZXFjhUpyEvNBTBocJ3,H7R08w7anBOSVpR9pmDicCp3X8a2"
# Secrets (set via `npx wrangler secret put`):
#   FIREBASE_SA_KEY       — service account JSON (for Firestore Admin access)
#   GEMINI_API_KEY        — shared Gemini key
#   STRIPE_SECRET_KEY     — Stripe sk_test_/sk_live_
#   STRIPE_WEBHOOK_SECRET — Stripe whsec_ (from `stripe listen` or dashboard)
